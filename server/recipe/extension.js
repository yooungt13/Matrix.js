/**
 * @author youngtian13
 * @date 2016-08-19
 */

'use strict';

module.exports = (app) => {
    const EXT_PATH = app.config.path.extension;

    // 添加系统扩展，如统计，日志
    // const A = require(EXT_PATH);
    // app.use(A);
};
